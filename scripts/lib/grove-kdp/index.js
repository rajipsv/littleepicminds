const { KDP } = require('./constants');
const { renderBookDocx } = require('./render-book');
const { renderDraftDocx } = require('./render-draft');

module.exports = {
  KDP,
  renderBookDocx,
  renderDraftDocx,
};
